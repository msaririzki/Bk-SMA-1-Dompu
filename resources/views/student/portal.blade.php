<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Data Saya — Sistem BK</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body><header class="bg-navy-950 text-white"><div class="container-page flex h-18 items-center justify-between"><a class="font-bold" href="{{ route('student.portal') }}">Portal Siswa · SMAN 1 Dompu</a><form method="post" action="{{ route('logout') }}">@csrf<button class="btn border border-white/20 text-white">Keluar</button></form></div></header><main class="container-page py-8">@if(session('success'))<div class="mb-5 rounded-xl bg-emerald-50 p-4 text-emerald-800">{{ session('success') }}</div>@endif<div class="mb-7"><p class="text-sm font-semibold text-teal-700">{{ $student->currentEnrollment?->schoolClass?->name ?? 'Kelas belum ditentukan' }}</p><h1 class="mt-1 text-3xl font-bold">Halo, {{ $student->name }}</h1><p class="mt-2 text-sm text-slate-500">Informasi di halaman ini bersifat pribadi.</p></div>@if($score)<div class="grid gap-4 sm:grid-cols-3"><div class="stat"><p class="stat-label">Total poin {{ $year->name }}</p><p class="stat-value">{{ $score['annual_points'] }}</p></div><div class="stat"><p class="stat-label">Progres ambang</p><p class="stat-value">{{ $score['percentage'] }}%</p><div class="mt-3 h-2 rounded-full bg-slate-100"><div class="h-2 rounded-full bg-teal-500" style="width:{{ $score['percentage'] }}%"></div></div></div><div class="stat"><p class="stat-label">Tingkat</p><p class="mt-3"><span class="badge badge-{{ $score['severity']?->color ?? 'emerald' }}">{{ $score['severity']?->name ?? 'Tidak ada pelanggaran' }}</span></p></div></div>@endif<section class="card mt-6"><div class="card-body"><h2 class="text-lg font-bold">Riwayat saya</h2><p class="text-sm text-slate-500">Catatan konseling, foto, dan dokumen internal tidak ditampilkan.</p></div><div class="divide-y divide-slate-100">@forelse($student->cases as $case)<div class="p-5"><div class="flex flex-wrap justify-between gap-3"><div><strong>{{ $case->occurred_at->translatedFormat('d F Y') }}</strong><p class="mt-1 text-sm text-slate-500">{{ $case->items->pluck('instrument_name')->join(', ') }}</p></div><div class="text-right"><span class="badge badge-amber">{{ $case->items->sum('points') }} poin</span><p class="mt-2 text-xs text-slate-400">{{ $case->status->label() }}</p></div></div></div>@empty<p class="p-10 text-center text-sm text-slate-400">Tidak ada catatan pelanggaran pada tahun ini.</p>@endforelse</div></section></main></body></html>
