@extends('layouts.app')
@section('title','Data Siswa')
@section('content')
<div class="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 class="text-3xl font-bold">Data siswa</h1><p class="mt-2 text-sm text-slate-500">Cari berdasarkan nama, NIS, NISN, atau ID sementara.</p></div>@if(auth()->user()->hasRole(\App\Enums\UserRole::SuperAdmin,\App\Enums\UserRole::Coordinator))<a class="btn btn-secondary" href="{{ route('imports.index') }}">Impor Excel</a>@endif</div>
<form class="card card-body mb-6 grid gap-4 md:grid-cols-[1fr_240px_170px_auto]"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Ketik nama, NIS, NISN, atau ID sementara"><select class="form-select" name="class_id"><option value="">Semua kelas</option>@foreach($classes as $class)<option value="{{ $class->id }}" @selected(request('class_id')==$class->id)>{{ $class->name }} — {{ $class->academicYear->name }}</option>@endforeach</select><select class="form-select" name="status"><option value="">Semua status</option>@foreach(\App\Enums\StudentStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><button class="btn btn-primary">Cari siswa</button></form>
<div class="card table-wrap"><table class="table"><thead><tr><th>Nama siswa</th><th>Identitas</th><th>Kelas</th><th>Status</th><th></th></tr></thead><tbody>@forelse($students as $student)<tr><td><strong>{{ $student->name }}</strong><p class="text-xs text-slate-400">{{ $student->gender==='L'?'Laki-laki':'Perempuan' }}</p></td><td><p>NISN: {{ $student->nisn ?? '-' }}</p><p class="text-xs text-slate-400">NIS: {{ $student->nis ?? '-' }} · {{ $student->temporary_id }}</p></td><td>{{ $student->currentEnrollment?->schoolClass?->name ?? '-' }}</td><td><span class="badge badge-{{ $student->status->value==='active'?'emerald':'slate' }}">{{ $student->status->label() }}</span></td><td class="text-right"><a class="btn btn-secondary" href="{{ route('students.show',$student) }}">Buka</a></td></tr>@empty<tr><td colspan="5" class="py-10 text-center text-slate-400">Data siswa tidak ditemukan.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $students->links() }}</div>
@endsection
