@extends('layouts.app')
@section('title','Audit Aktivitas')
@section('content')
<header class="page-header"><div><p class="page-eyebrow"><x-icon name="shield" class="size-4" /> Keamanan sistem</p><h1 class="page-title">Audit aktivitas</h1><p class="page-description">Jejak perubahan sensitif beserta pengguna, waktu, IP, dan nilai sebelum/sesudah.</p></div><form class="w-full sm:w-72"><label class="form-label" for="audit-search">Cari aktivitas</label><div class="input-with-icon"><x-icon name="search" /><input id="audit-search" class="form-control" name="q" value="{{ request('q') }}" placeholder="Aksi atau objek..."></div></form></header>
<section class="card table-wrap"><table class="table"><thead><tr><th>Waktu</th><th>Pengguna</th><th>Aksi</th><th>Objek</th><th>Alasan</th><th>IP</th></tr></thead><tbody>@forelse($logs as $log)<tr><td class="whitespace-nowrap">{{ $log->created_at->translatedFormat('d M Y H:i:s') }}</td><td>{{ $log->user?->name ?? 'Sistem' }}</td><td><span class="badge badge-slate">{{ $log->action }}</span></td><td><p class="max-w-64 truncate text-xs">{{ class_basename($log->subject_type) }} {{ $log->subject_id }}</p>@if($log->before||$log->after)<details class="mt-2 text-xs"><summary class="cursor-pointer font-semibold text-teal-700">Lihat perubahan</summary><pre class="mt-2 max-w-md overflow-auto rounded-lg bg-slate-950 p-3 text-slate-100">{{ json_encode(['sebelum'=>$log->before,'sesudah'=>$log->after],JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></details>@endif</td><td>{{ $log->reason ?: '-' }}</td><td>{{ $log->ip_address ?: '-' }}</td></tr>@empty<tr><td colspan="6" class="text-center text-slate-400">Belum ada aktivitas.</td></tr>@endforelse</tbody></table></section><div class="mt-5">{{ $logs->links() }}</div>
@endsection
