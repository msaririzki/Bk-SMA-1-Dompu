@extends('layouts.app')
@section('title',$document->type->label())
@section('content')
<header class="page-header"><div><a class="back-link" href="{{ route('documents.index') }}"><x-icon name="arrow-left" /> Arsip dokumen</a><p class="page-eyebrow"><x-icon name="documents" class="size-4" /> Detail dokumen</p><h1 class="page-title">{{ $document->type->label() }}</h1><p class="page-description">{{ $document->student->name }} · {{ $document->document_date->translatedFormat('d F Y') }}</p></div><a class="btn btn-primary" href="{{ route('documents.pdf',$document) }}"><x-icon name="documents" /> Unduh PDF</a></header>
<section class="card card-body mx-auto max-w-4xl"><div class="grid gap-4 sm:grid-cols-3"><div><p class="text-xs font-bold uppercase text-slate-400">Nomor</p><p class="mt-1">{{ $document->number ?: '-' }}</p></div><div><p class="text-xs font-bold uppercase text-slate-400">Siswa</p><p class="mt-1">{{ $document->student->name }}</p></div><div><p class="text-xs font-bold uppercase text-slate-400">Kelas</p><p class="mt-1">{{ $document->student->currentEnrollment?->schoolClass?->name ?? '-' }}</p></div></div>@if($document->homeVisit)<div class="mt-8 space-y-6"><div><h2 class="font-bold">Permasalahan konseli</h2><p class="mt-2 whitespace-pre-line text-sm leading-7 text-slate-600">{{ $document->homeVisit->problem }}</p></div><div><h2 class="font-bold">Tujuan</h2><p class="mt-2 whitespace-pre-line text-sm leading-7 text-slate-600">{{ $document->homeVisit->purpose }}</p></div><div><h2 class="font-bold">Hasil</h2><p class="mt-2 whitespace-pre-line text-sm leading-7 text-slate-600">{{ $document->homeVisit->result }}</p></div><div><h2 class="font-bold">Tindak lanjut</h2><p class="mt-2 whitespace-pre-line text-sm leading-7 text-slate-600">{{ $document->homeVisit->follow_up }}</p></div></div>@else<div class="mt-8"><h2 class="text-xl font-bold">{{ $document->payload['subject'] ?? $document->type->label() }}</h2><p class="mt-5 whitespace-pre-line text-sm leading-8 text-slate-700">{{ $document->payload['body'] ?? '' }}</p></div>@endif</section>
@endsection
