@extends('layouts.app')
@section('title','Pelanggaran')
@section('content')
<div class="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 class="text-3xl font-bold">Pencatatan pelanggaran</h1><p class="mt-2 text-sm text-slate-500">Seluruh kejadian, poin, bukti, dan tindak lanjut.</p></div><a class="btn btn-primary" href="{{ route('cases.create') }}">+ Catat pelanggaran</a></div>
<form class="card card-body mb-6 grid gap-4 md:grid-cols-[1fr_220px_auto]"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Cari nama, NIS, atau NISN"><select class="form-select" name="status"><option value="">Semua status</option>@foreach(\App\Enums\CaseStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><button class="btn btn-primary">Terapkan</button></form>
<div class="card table-wrap"><table class="table"><thead><tr><th>Nomor/tanggal</th><th>Siswa</th><th>Pelanggaran</th><th>Poin</th><th>Status</th><th></th></tr></thead><tbody>@forelse($cases as $case)<tr><td><strong>{{ $case->case_number }}</strong><p class="text-xs text-slate-400">{{ $case->occurred_at->translatedFormat('d M Y H:i') }}</p></td><td><strong>{{ $case->student->name }}</strong><p class="text-xs text-slate-400">{{ $case->student->currentEnrollment?->schoolClass?->name }}</p></td><td><p class="max-w-sm line-clamp-2">{{ $case->items->pluck('instrument_name')->join(', ') }}</p></td><td><span class="badge badge-amber">{{ $case->items->sum('points') }}</span></td><td><span class="badge badge-slate">{{ $case->status->label() }}</span></td><td class="text-right"><a class="btn btn-secondary" href="{{ route('cases.show',$case) }}">Buka</a></td></tr>@empty<tr><td colspan="6" class="py-10 text-center text-slate-400">Belum ada kasus.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $cases->links() }}</div>
@endsection
